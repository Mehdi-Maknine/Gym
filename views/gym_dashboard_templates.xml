<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <template id="gym_dashboard_template" name="Gym Dashboard">
        <t t-call="website.layout">
            <div class="container py-5">
                <h2 class="text-center mb-5">
                    <span class="me-2">🏋️‍♂️</span> Gym Dashboard
                </h2>

                <div class="row g-4 text-center">

                    <!-- Metric Card -->
                    <t t-foreach="[{
                        'label': 'Active Members',
                        'value': active_members,
                        'icon': 'fa-users',
                        'color': 'success'
                    }, {
                        'label': 'Upcoming Sessions',
                        'value': upcoming_sessions,
                        'icon': 'fa-calendar-alt',
                        'color': 'info'
                    }, {
                        'label': 'Expiring Soon',
                        'value': expiring_members,
                        'icon': 'fa-exclamation-triangle',
                        'color': 'warning'
                    }, {
                        'label': 'Total Revenue',
                        'value': total_payments,
                        'icon': 'fa-euro-sign',
                        'color': 'primary'
                    }]" t-as="metric">
                        <div class="col-12 col-sm-6 col-md-3">
                            <div class="card shadow border border-2 border-#{metric['color']} h-100 hover-shadow">
                                <div class="card-body">
                                    <i class="fa #{metric['icon']} fa-2x text-#{metric['color']} mb-3 d-block"></i>
                                    <h5 class="card-title"><t t-esc="metric['label']"/></h5>
                                    <p class="fs-3 fw-bold text-#{metric['color']}">
                                        <t t-esc="metric['value']"/>
                                    </p>
                                </div>
                            </div>
                        </div>
                    </t>

                </div>

                <div class="text-center mt-5">
                    <a href="/my" class="btn btn-outline-secondary btn-lg">
                        <i class="fa fa-arrow-left me-1"></i> Return to Portal
                    </a>
                </div>

                <!-- Safe embedded CSS using CDATA -->
                <style type="text/css">
                    <![CDATA[
                        .hover-shadow:hover {
                            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2) !important;
                            transform: scale(1.02);
                            transition: all 0.3s ease;
                        }
                    ]]>
                </style>
            </div>
        </t>
    </template>
</odoo>
